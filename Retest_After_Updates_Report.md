# 三个用例复测报告（应用今天的更新后）
# Re-test Report — 766481 / 775368 / 776060 after today's updates

对每个用例重新跑完整流程（SoW + SLD，排除答案/BOQ 文件），与业务真实 BOQ 对比结论：**结构与范围的差距明显缩小；数量与"按电压/盘"分层仍待改进。**

---

## 结论一句话
- **之前**：扁平的"品类×数量"清单，缺配套/服务，含错误范围与错误分类（iQ+×14、GIS 局放、变压器测温）。
- **现在**：接近真实 BOQ 的**工程化结构**——录波 + 测试开关 + GPS + 交换机 + 防火墙 + 盘柜 + EPG 授权 + LEV/PDC 全套 + 服务；软件恢复 1/系统；纯图纸不再空结果。
- **仍差**：录波数量偏大（按回路数而非被监测间隔）、录波仍分散在 3 个旧产品族、未按电压等级/盘分层、776060 仍误报一条变压器测温。

---

## 766481（只有 SLD、扫描件）
| | 之前 | 现在 |
|---|---|---|
| 结果 | **完全空**（No scope detected） | VLM 成功读图 → 检出 GIS_PD 场景 + 1 行草稿 + 澄清问题 |
说明：纯设备型 SLD 本身不含监测范围信息，所以仍很少——但**从"空"变成"可复核草稿 + 需补 SoW 提示"**。无对应 actual BOQ。

## 775368（Habshan，SoW+SLD）
- 行数：**8 → 32**。
- 录波：HATHAWAY IDM(PMU) 35、IDM+9(DFR) 12、BEN 6000(PQ) 35。
- ✅ **iQ+ 恢复为软件 ×1**（不再 ×14）；✅ **本次未再报 GIS 局放/变压器测温**（范围收窄）。
- ✅ 新增全套配套：测试开关、L2/L3 交换机、防火墙、FMS 盘、GPS 天线+分配器、EPG 授权、LEV 柜(工控机/杀毒/白名单/备份/显示器/打印机/报警器)、PDC 柜(eSPDC/KVM)、7 项服务。
- 对比 actual（按盘的 IDM+/Informa + LEV/PDC + GPS + 交换机 + EPG + 服务）：**品类覆盖从 ~25% 提升到 ~80%**。
- 仍差：PMU 应 ~14（现 35）、PQ 应 ~9（现 35）、录波分散在 3 个族（应统一 IDM+/Informa）、未分 400/220kV 与盘。

## 776060（KIZAD，SoW+2×SLD）
- 行数：**→ 33**，结构同上（录波 + 全套配套 + 服务）。
- 录波：HATHAWAY IDM 66、IDM+9 22、BEN 6000 66；配套/服务齐全，iQ+=1。
- 对比 actual（132kV/11kV 分开的 FMS/PQM/PMU，每间隔一台 IDM+ 9A/32D + LEV/PDC）：**结构大幅接近**。
- 仍差：数量明显偏大（应 132kV~10 + 11kV~8，现 66）、未分电压等级、仍误报 1 条变压器测温（QUALITROL QTMS ×6，SoW 提到变压器触发）。

---

## 差距缩小 vs 仍需改进（汇总）
**已明显缩小：**
1. 配套/网络/盘柜/软件授权/服务——从"几乎没有"到"基本齐全"（差距最大的一块）。
2. 软件/服务器 = 1/系统（iQ+ 修复）。
3. 纯图纸不再空结果（VLM 读图 + 澄清）。
4. 上下文/反馈可进一步收窄范围（已另行验证）。

**仍需改进（下一步）：**
1. **数量基准**：录波按"被监测间隔数/电压等级"而非回路标签数（现在偏大约 2–4 倍，且会带动 EPG/盘/测试开关等配套一起偏大）。
2. **统一录波型号**：把 FMS/PQM/PMU 统一到 IDM+/Informa（现在仍分散在 HATHAWAY IDM / IDM+ / BEN 6000 三个旧族）。
3. **分层结构**：按电压等级 → 功能 → 盘 输出，贴近真实 BOQ 版式。
4. **残留误报**：776060 的变压器测温应剔除（可用场景门槛或反馈）。
